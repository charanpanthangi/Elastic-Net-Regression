# Examples

- Run the full pipeline:
  ```bash
  python app/main.py
  ```
- Adjust hyperparameters in `app/model.py` by changing `alpha` and `l1_ratio` to see how sparsity and error metrics change.
- Use the notebook `notebooks/demo_elastic_net_regression.ipynb` for an interactive walkthrough.
